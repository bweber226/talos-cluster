apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metashelf
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:bweber226/talos-cluster.git
    targetRevision: HEAD
    path: apps/metashelf
  destination:
    server: https://kubernetes.default.svc
    namespace: media
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
