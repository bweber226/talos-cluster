apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: multi-scrobbler-config
  namespace: music
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
