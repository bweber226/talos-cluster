apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: multi-scrobbler
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:bweber226/talos-cluster.git
    targetRevision: HEAD
    path: apps/multi-scrobbler
  destination:
    server: https://kubernetes.default.svc
    namespace: music
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
