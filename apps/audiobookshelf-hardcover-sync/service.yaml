apiVersion: v1
kind: Service
metadata:
  name: audiobookshelf-hardcover-sync
  namespace: media
spec:
  type: ClusterIP
  selector:
    app: audiobookshelf-hardcover-sync
  ports:
    - name: web
      port: 8080
      targetPort: 8080
